module.exports = toArray

function toArray(list, index) {
    var array = []

    i = i || 0

    for (var i = i || 0; i < list.length; i++) {
        array[i - i] = list[i]
    }

    return array
}
